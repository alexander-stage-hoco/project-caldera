# PoC #1: scc - Python dependencies
# Minimal dependencies for standalone operation

# JSON Schema validation (optional but recommended)
jsonschema>=4.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Numerical validation for percentile tests
numpy>=1.26.0

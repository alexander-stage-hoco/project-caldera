# Layout Scanner - Dependencies
# Core dependencies
pathspec>=0.11.0  # Gitignore pattern matching with proper ** semantics
pyyaml>=6.0.0     # YAML parsing for classification rules

# Optional dependencies for enhanced language detection:
# enry>=0.1.1  # Go-enry Python bindings (requires Go compiler to install)
# Note: The built-in detection includes shebang parsing, extension and filename
# detection. Enry adds modeline detection, content heuristics, and Bayesian classifier.

# Optional dependencies for development/testing:
jsonschema>=4.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

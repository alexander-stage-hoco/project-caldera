select
    run_pk,
    file_id,
    directory_id,
    relative_path,
    filename,
    extension,
    language,
    lines_total,
    code_lines,
    comment_lines,
    blank_lines,
    bytes,
    complexity,
    uloc,
    comment_ratio,
    blank_ratio,
    code_ratio,
    complexity_density,
    dryness,
    bytes_per_loc,
    is_minified,
    is_generated,
    is_binary,
    classification
from {{ source('lz', 'lz_scc_file_metrics') }}
